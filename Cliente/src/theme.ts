// theme.js

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff5722', // por exemplo, escolha uma cor principal para o seu tema
    },
    secondary: {
      main: '#00bcd4', // por exemplo, escolha uma cor secundária para o seu tema
    },
  },
  // outras opções de configuração do tema, se necessário
});

export default theme;
